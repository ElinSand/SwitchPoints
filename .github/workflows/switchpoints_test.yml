name: Build and Run

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  buil-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      
 
